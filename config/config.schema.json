{
  "$defs": {
    "ExecutionBackendType": {
      "description": "Execution backend for container lifecycle operations.",
      "enum": [
        "docker_sdk",
        "docker",
        "noop"
      ],
      "title": "ExecutionBackendType",
      "type": "string"
    },
    "LangfuseProjectCreds": {
      "description": "Credentials for one Langfuse trace project.",
      "properties": {
        "public_key": {
          "default": "",
          "description": "Langfuse public key for the project.",
          "title": "Public Key",
          "type": "string"
        },
        "secret_key": {
          "default": "",
          "description": "Langfuse secret key for the project.",
          "format": "password",
          "title": "Secret Key",
          "type": "string",
          "writeOnly": true
        }
      },
      "title": "LangfuseProjectCreds",
      "type": "object"
    },
    "OvhSftpConfig": {
      "description": "OVH website SFTP credentials, seeded into the encrypted env store on first boot.",
      "properties": {
        "host": {
          "default": "",
          "description": "OVH SFTP hostname.",
          "title": "Host",
          "type": "string"
        },
        "port": {
          "default": 22,
          "description": "OVH SFTP port.",
          "title": "Port",
          "type": "integer"
        },
        "user": {
          "default": "",
          "description": "OVH SFTP username.",
          "title": "User",
          "type": "string"
        },
        "password": {
          "default": "",
          "description": "OVH SFTP password.",
          "format": "password",
          "title": "Password",
          "type": "string",
          "writeOnly": true
        }
      },
      "title": "OvhSftpConfig",
      "type": "object"
    },
    "StoreBackend": {
      "description": "Persistence backend for the lifecycle service store.",
      "enum": [
        "memory",
        "file"
      ],
      "title": "StoreBackend",
      "type": "string"
    },
    "VirtualComponentEntry": {
      "description": "Minimal spec for a virtual (non-Docker) chat-accessible component.",
      "properties": {
        "id": {
          "pattern": "^[a-z0-9][a-z0-9-]*$",
          "title": "Id",
          "type": "string"
        },
        "chat_base_url": {
          "default": "",
          "title": "Chat Base Url",
          "type": "string"
        },
        "chat_skill_endpoint": {
          "default": "/chat-skill",
          "title": "Chat Skill Endpoint",
          "type": "string"
        },
        "chat_skill": {
          "default": "",
          "title": "Chat Skill",
          "type": "string"
        },
        "auth_type": {
          "default": "",
          "title": "Auth Type",
          "type": "string"
        },
        "auth_header_name": {
          "default": "",
          "title": "Auth Header Name",
          "type": "string"
        },
        "auth_username_env": {
          "default": "",
          "title": "Auth Username Env",
          "type": "string"
        },
        "auth_password_env": {
          "default": "",
          "description": "env var holding Basic-Auth password",
          "format": "password",
          "title": "Auth Password Env",
          "type": "string",
          "writeOnly": true
        },
        "auth_token_env": {
          "default": "",
          "title": "Auth Token Env",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "title": "VirtualComponentEntry",
      "type": "object"
    }
  },
  "description": "Configuration for the lifecycle server.",
  "properties": {
    "host": {
      "advanced": true,
      "default": "0.0.0.0",
      "description": "Interface the HTTP server binds to.",
      "title": "Host",
      "type": "string"
    },
    "port": {
      "advanced": true,
      "default": 8100,
      "description": "Port the HTTP server listens on.",
      "title": "Port",
      "type": "integer"
    },
    "api_key": {
      "default": "",
      "description": "Legacy API credential accepted via the X-API-Key header or as a Basic-auth password. Empty disables api-key auth.",
      "format": "password",
      "title": "Api Key",
      "type": "string",
      "writeOnly": true
    },
    "auth_username": {
      "default": "",
      "description": "Basic-auth username for the dashboard and API. Auth is enforced only when both username and password are set (or api_key is).",
      "title": "Auth Username",
      "type": "string"
    },
    "auth_password": {
      "default": "",
      "description": "Basic-auth password paired with auth_username.",
      "format": "password",
      "title": "Auth Password",
      "type": "string",
      "writeOnly": true
    },
    "store_backend": {
      "$ref": "#/$defs/StoreBackend",
      "advanced": true,
      "default": "memory",
      "description": "Persistence backend for service records: 'memory' is ephemeral (dev), 'file' persists to store_path."
    },
    "store_path": {
      "advanced": true,
      "default": "lifecycle_state.yaml",
      "description": "Path of the service-record store (file backend only).",
      "title": "Store Path",
      "type": "string"
    },
    "execution_backend": {
      "$ref": "#/$defs/ExecutionBackendType",
      "advanced": true,
      "default": "docker_sdk",
      "description": "How containers are managed: 'docker_sdk' (full support), 'docker' CLI (status/logs only), or 'noop' (dry runs)."
    },
    "component_config_store_path": {
      "advanced": true,
      "default": "data/component_configs.json",
      "description": "Path of the persisted per-component deployment configs.",
      "title": "Component Config Store Path",
      "type": "string"
    },
    "docker_socket_url": {
      "default": "unix:///var/run/docker.sock",
      "description": "Docker API endpoint. Production runs behind a socket proxy: tcp://socket-proxy:2375.",
      "title": "Docker Socket Url",
      "type": "string"
    },
    "docker_sdk_timeout": {
      "advanced": true,
      "default": 120,
      "description": "Client-level timeout (seconds) for every Docker SDK operation (pull, create, start, stop, \u2026). The default accommodates typical image pulls.",
      "title": "Docker Sdk Timeout",
      "type": "integer"
    },
    "disk_path": {
      "advanced": true,
      "default": "/",
      "description": "Filesystem path whose free space is reported on the dashboard; /host_root when containerised (host filesystem mount).",
      "title": "Disk Path",
      "type": "string"
    },
    "disk_warn_pct": {
      "advanced": true,
      "default": 10.0,
      "description": "Warn on the dashboard when free disk space drops below this percentage.",
      "title": "Disk Warn Pct",
      "type": "number"
    },
    "env_store_path": {
      "advanced": true,
      "default": "component_env.json",
      "description": "Path of the per-component env/secret store.",
      "title": "Env Store Path",
      "type": "string"
    },
    "secret_key_path": {
      "advanced": true,
      "default": "secrets.key",
      "description": "Path of the Fernet key encrypting stored secrets.",
      "title": "Secret Key Path",
      "type": "string"
    },
    "config_yaml_store_path": {
      "advanced": true,
      "default": "data/component_config_yaml.json",
      "description": "Path of the per-component config template/values store.",
      "title": "Config Yaml Store Path",
      "type": "string"
    },
    "deploy_history_store_path": {
      "advanced": true,
      "default": "data/deploy_history.json",
      "description": "Path of the per-component deploy-history JSON store.",
      "title": "Deploy History Store Path",
      "type": "string"
    },
    "self_update_watchtower_image": {
      "advanced": true,
      "default": "containrrr/watchtower:1.7.1",
      "description": "One-shot updater image launched by the dashboard's 'Update server' button; keep the tag pinned.",
      "title": "Self Update Watchtower Image",
      "type": "string"
    },
    "self_update_docker_api_version": {
      "advanced": true,
      "default": "1.44",
      "description": "DOCKER_API_VERSION exported to the one-shot updater. Watchtower 1.7.1's client defaults to API 1.25, below modern daemons' minimum, and panics without this.",
      "title": "Self Update Docker Api Version",
      "type": "string"
    },
    "registry_check_ttl": {
      "advanced": true,
      "default": 300,
      "description": "Cache TTL (seconds) for registry manifest-digest lookups.",
      "title": "Registry Check Ttl",
      "type": "integer"
    },
    "registry_check_interval": {
      "advanced": true,
      "default": 300,
      "description": "Interval (seconds) of the background update-available check; 0 disables it.",
      "title": "Registry Check Interval",
      "type": "integer"
    },
    "system_settings_path": {
      "advanced": true,
      "default": "data/system_settings.json",
      "description": "Path of the operator-editable System Settings store.",
      "title": "System Settings Path",
      "type": "string"
    },
    "self_contract_path": {
      "advanced": true,
      "default": "deploy/docker-compose.yml",
      "description": "Path to central-deploy's own deploy contract (deploy/docker-compose.yml). Read at startup to seed system settings from contract labels (robotsix.deploy.settings.*).",
      "title": "Self Contract Path",
      "type": "string"
    },
    "log_level": {
      "default": "INFO",
      "description": "Root log level: DEBUG, INFO, WARNING, ERROR, or CRITICAL.",
      "title": "Log Level",
      "type": "string"
    },
    "gateway_base_domain": {
      "default": "",
      "description": "Base domain for the subdomain gateway (e.g. deploy.robotsix.net routes <component>.deploy.robotsix.net). Empty disables subdomain routing.",
      "title": "Gateway Base Domain",
      "type": "string"
    },
    "volume_audit_enabled": {
      "default": false,
      "description": "Master switch for the periodic volume-growth audit.",
      "title": "Volume Audit Enabled",
      "type": "boolean"
    },
    "volume_audit_interval_seconds": {
      "advanced": true,
      "default": 3600,
      "description": "Interval (seconds) between volume-audit scans.",
      "title": "Volume Audit Interval Seconds",
      "type": "integer"
    },
    "volume_audit_snapshot_path": {
      "advanced": true,
      "default": "data/volume_audit_snapshots.json",
      "description": "Path of persisted volume-size snapshots.",
      "title": "Volume Audit Snapshot Path",
      "type": "string"
    },
    "volume_audit_findings_path": {
      "advanced": true,
      "default": "data/volume_audit_findings.json",
      "description": "Path of persisted volume-audit findings.",
      "title": "Volume Audit Findings Path",
      "type": "string"
    },
    "volume_audit_growth_threshold_pct": {
      "advanced": true,
      "default": 10.0,
      "description": "Volume growth (percent between scans) above which a finding is raised.",
      "title": "Volume Audit Growth Threshold Pct",
      "type": "number"
    },
    "volume_audit_min_delta_bytes": {
      "advanced": true,
      "default": 10485760,
      "description": "Minimum absolute growth (bytes) before a finding is raised \u2014 filters noise on small volumes. Default 10 MiB.",
      "title": "Volume Audit Min Delta Bytes",
      "type": "integer"
    },
    "board_api_url": {
      "advanced": true,
      "default": "",
      "description": "Board API base URL for filing audit-finding tickets; empty disables.",
      "title": "Board Api Url",
      "type": "string"
    },
    "board_api_token": {
      "advanced": true,
      "default": "",
      "description": "Bearer token for the board API.",
      "format": "password",
      "title": "Board Api Token",
      "type": "string",
      "writeOnly": true
    },
    "board_repo_id": {
      "advanced": true,
      "default": "",
      "description": "Board repo id under which audit tickets are filed.",
      "title": "Board Repo Id",
      "type": "string"
    },
    "github_app_id": {
      "advanced": true,
      "default": "",
      "description": "GitHub App ID used to mint installation tokens for the chat agent's 'github' component. Empty disables the component.",
      "format": "password",
      "title": "Github App Id",
      "type": "string",
      "writeOnly": true
    },
    "github_app_private_key": {
      "advanced": true,
      "default": "",
      "description": "GitHub App private key (PEM) paired with github_app_id.",
      "format": "password",
      "title": "Github App Private Key",
      "type": "string",
      "writeOnly": true
    },
    "installation_id": {
      "advanced": true,
      "default": "",
      "description": "GitHub App installation ID for the fleet's shared installation. Used together with github_app_id and github_app_private_key to mint short-lived installation access tokens.",
      "format": "password",
      "title": "Installation Id",
      "type": "string",
      "writeOnly": true
    },
    "github_repo_create_token": {
      "advanced": true,
      "default": "",
      "description": "A GitHub Personal Access Token (classic 'repo' scope, or fine-grained with Administration:read-and-write) used only for POST /chat/github/repos. GitHub App installation tokens cannot create repositories under a personal account ('Resource not accessible by integration'), so repo creation needs a separate PAT. Empty disables repo creation (the Actions-status endpoints are unaffected).",
      "format": "password",
      "title": "Github Repo Create Token",
      "type": "string",
      "writeOnly": true
    },
    "langfuse_projects": {
      "additionalProperties": {
        "$ref": "#/$defs/LangfuseProjectCreds"
      },
      "advanced": true,
      "description": "Langfuse project alias \u2192 credentials mapping.  Example: {\"my-project\": {\"public_key\": \"pk-...\", \"secret_key\": \"sk-...\"}}.",
      "title": "Langfuse Projects",
      "type": "object"
    },
    "langfuse_base_url": {
      "advanced": true,
      "default": "",
      "description": "Langfuse instance base URL (no trailing slash).",
      "title": "Langfuse Base Url",
      "type": "string"
    },
    "caretaker_enabled": {
      "default": false,
      "description": "Enable the periodic caretaker pass (auto-update, health, and volume checks).",
      "title": "Caretaker Enabled",
      "type": "boolean"
    },
    "caretaker_interval_hours": {
      "advanced": true,
      "default": 24,
      "description": "Hours between caretaker passes.",
      "title": "Caretaker Interval Hours",
      "type": "integer"
    },
    "mill_component_id": {
      "advanced": true,
      "default": "",
      "description": "Component id of the mill instance the caretaker reports findings to.",
      "title": "Mill Component Id",
      "type": "string"
    },
    "image_auto_prune": {
      "advanced": true,
      "default": false,
      "description": "After updates, remove dangling images not needed for rollback.",
      "title": "Image Auto Prune",
      "type": "boolean"
    },
    "llmio_tier_config": {
      "additionalProperties": true,
      "advanced": true,
      "default": {},
      "description": "Fleet-global mapping from llmio capability level (level1-4) to provider and model. Overridden by the System Settings store.",
      "title": "Llmio Tier Config",
      "type": "object"
    },
    "chat_agent_audit_store_path": {
      "advanced": true,
      "default": "data/chat_agent_audit.json",
      "description": "Path of the chat-agent mutation audit log.",
      "title": "Chat Agent Audit Store Path",
      "type": "string"
    },
    "claude_auth_refresh_interval": {
      "advanced": true,
      "default": 1800,
      "description": "Interval (seconds) between Claude auth credential refresh attempts; 0 disables background refresh.",
      "title": "Claude Auth Refresh Interval",
      "type": "integer"
    },
    "rate_limit_login_per_minute": {
      "advanced": true,
      "default": 10,
      "description": "Max POST /login requests per IP per minute.",
      "title": "Rate Limit Login Per Minute",
      "type": "integer"
    },
    "rate_limit_api_per_hour": {
      "advanced": true,
      "default": 20000,
      "description": "Max API requests per IP per hour. Must accommodate the dashboard UI, which polls several endpoints every few seconds from one IP (~5000/h per open tab).",
      "title": "Rate Limit Api Per Hour",
      "type": "integer"
    },
    "rate_limit_login_max_attempts": {
      "advanced": true,
      "default": 20,
      "description": "Failed login attempts before IP lockout.",
      "title": "Rate Limit Login Max Attempts",
      "type": "integer"
    },
    "rate_limit_login_lockout_seconds": {
      "advanced": true,
      "default": 300,
      "description": "Lockout duration (seconds) after too many failed logins.",
      "title": "Rate Limit Login Lockout Seconds",
      "type": "integer"
    },
    "csrf_secret": {
      "advanced": true,
      "default": "",
      "description": "Secret key for CSRF token signing. Auto-generated (random) when empty, which invalidates outstanding tokens on every restart \u2014 acceptable for single-server deployments.",
      "format": "password",
      "title": "Csrf Secret",
      "type": "string",
      "writeOnly": true
    },
    "chat_agent_deployable_components": {
      "description": "Component names the chat agent is allowed to deploy via the generic POST /chat/deploy endpoint. Each entry must match ^[a-z0-9][a-z0-9-]*$. Distinct from chat_agent_mutatable (per-component flag) \u2014 this is a server-level allowlist for components that may not have a persisted ComponentConfig yet.",
      "items": {
        "type": "string"
      },
      "title": "Chat Agent Deployable Components",
      "type": "array"
    },
    "virtual_components": {
      "advanced": true,
      "description": "Virtual (non-Docker) components to register in the chat-agent component roster alongside onboarded Docker services.",
      "items": {
        "$ref": "#/$defs/VirtualComponentEntry"
      },
      "title": "Virtual Components",
      "type": "array"
    },
    "ovh_sftp": {
      "$ref": "#/$defs/OvhSftpConfig",
      "description": "OVH website SFTP credentials. If all four fields are set AND the ovh-website-credentials entry does not already exist in the encrypted store, the values are seeded at startup with scope tag 'website:ovh'."
    }
  },
  "title": "LifecycleConfig",
  "type": "object"
}


