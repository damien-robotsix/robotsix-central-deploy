Deduplicate copy-pasted Field clusters in `lifecycle/schemas.py` via private base models: `_JobStatusFields` (onboard/deploy job status), `_DeployResultBase` (chat-agent update/deploy results), `_EnvSecretsFields` (env-update requests). No contract change — property sets, required lists, and types are identical; only description wording was unified where models diverged accidentally.
