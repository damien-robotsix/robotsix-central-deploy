central-deploy now schedules the fleet-wide coherent-set pin-bump sweep (Mondays 06:00 UTC, plus manual dispatch), calling `robotsix-github-workflows`' `pin-bump-sweep.yml` with the fleet App pair it already holds for release-please. The shared workflow is `workflow_call`-only and holds no credentials of its own, so without a caller the sweep could never run — which is why fleet pins had drifted up to eight weeks.
