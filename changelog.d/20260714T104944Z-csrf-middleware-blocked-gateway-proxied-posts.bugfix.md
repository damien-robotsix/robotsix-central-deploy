Fixed the CSRF middleware (introduced with the login/settings CSRF protection) rejecting unsafe-method requests to gateway-proxied component subdomains with "CSRF token verification failed" — e.g. sending a chat message through `chat.<base-domain>` was blocked. CSRF enforcement is now skipped for requests whose Host is a component subdomain; proxied apps handle their own CSRF protection.
