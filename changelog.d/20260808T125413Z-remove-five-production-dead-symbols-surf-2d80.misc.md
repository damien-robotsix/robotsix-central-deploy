Remove four production-dead symbols surfaced by the vulture gate: `run_config_assist` (abstract + all three backend implementations; the config-assist API route is a deprecated 410 stub), `ComponentRegistry.from_yaml` + `RegistryLoadError` (production builds the registry in memory), `_canonical_hash`, and the never-read `SecretKeyManager._key_path`. `GithubToken.token_type` was found to be load-bearing (PyGithub reads the override to emit `Authorization: Bearer`) and stays, recategorized in the whitelist.
