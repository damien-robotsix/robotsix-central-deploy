Claude auth: replace the broken helper-container login flow with a direct PKCE OAuth flow handled by central-deploy (link out → paste code → server exchanges tokens and writes `.credentials.json` into the `claude-auth` volume). Remove the host-seed migration and the `claude_auth_helper_image` setting; chown the volume to 1000:1000 on creation; onboarding checkbox now just says "Mount Claude credentials".
