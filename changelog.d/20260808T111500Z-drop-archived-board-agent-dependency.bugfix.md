Dropped the `robotsix-board-agent` git dependency, which was archived and made private on 2026-08-08 and could no longer be fetched — every CI job failed at dependency install with `could not read Username for 'https://github.com'`. The two calls the volume audit made (`create_ticket`, `close`) are now a small in-repo client against the same `POST /tickets` wire format, so existing `board_api_*` config keeps working unchanged.
