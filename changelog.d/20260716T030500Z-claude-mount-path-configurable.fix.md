Fix: the claude-auth volume bind is now configurable per component (`claude_mount_path`, or a path value on the `robotsix.deploy.claude-mount` label) instead of hardcoded to `/home/app/.claude`. Mill's image runs as user `mill` (home `/home/mill`), so since its 07-14 redeploy the bundled Claude CLI found no credentials and every Claude SDK call returned the degenerate "Not logged in" / `error result: success` envelope — silently degrading all mill refines and deadlocking ticket e43c.
