Add vulture dead-code detection as a CI gate (`uv run vulture` in the test job): decorator-registered FastAPI handlers and pydantic validators are exempted via `ignore_decorators`, remaining false positives live in `vulture_whitelist.py`. The first run already removed three dead spots (unused `_build_image` parameter, unused `stderr2` capture, never-read ingest counters in the caretaker scheduler).
