Caretaker: never auto-deploy the container it runs inside. Auto-updating the central-deploy self-component stopped and recreated the caretaker's own process mid-pass, so `update_available=False` was never persisted and every replacement self-updated again in an unbreakable boot loop (2026-07-21 outage of deploy.robotsix.net). Self-update remains available via `POST /system/update` (detached watchtower).
