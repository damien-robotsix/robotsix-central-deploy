Bump the robotsix-ui assets fetched at build time to v0.1.41, whose config client refuses a `/config` response that carries no `config` document instead of rendering — and then saving — schema defaults over a component's live config.
