Image auto-prune now runs on every caretaker cycle (not only after caretaker-applied updates), and `POST /disk/reclaim` additionally prunes unprotected dangling images. The dashboard disk panel shows dangling-image size and offers the Reclaim button whenever cache or dangling images are reclaimable. Dangling images had accumulated ~14.6 GB because most image pulls happen outside the deploy path. Also repoints the OpenAPI generator and CI drift check at `docs/lifecycle/openapi.json` (stale `docs/openapi.json` path since the per-module reorg made the check vacuous).
