Virtual (non-Docker) components (`langfuse`, `deploy`) no longer leak into the dashboard's tracked Component Status table after a server restart. `ComponentConfig` gained an `is_virtual` flag; the startup seeding loop in `lifecycle/deps.py` now skips (and deletes any stale) `ServiceRecord` for virtual components instead of treating every persisted `component_config_store` entry as a real Docker component.
