- Register Langfuse (`https://langfuse.robotsix.net`) as an external chat-agent component with a read-only public-API skill document, credential management endpoints (``PUT/GET /chat/credentials/{component_id}``), and a server-side proxy (``GET /chat/proxy/{component_id}/{path}``) that injects HTTP Basic Auth from EnvStore-stored credentials without surfacing them in chat. Multi-project support via ``?project=chat|cognee`` query parameter.
