{"user_facing": true, "summary": "No user-facing docs required to change. The diff is a data-only registration of the `cost-monitor` entry in the live virtual-component roster (`config/config.json`), which backs `GET /chat/components`. Verified: entry has `id: \"cost-monitor\"`, internal base URL `http://cost-monitor:8200` (Docker network hostname + port), live skill probing at `/chat-skill`, and no secrets (all auth fields empty). No doc enumerates the specific roster entries, so no markdown update was needed; release notes were already added (CHANGELOG entry + `changelog.d/` fragment). Noted in the memory ledger: `config/config.example.json` is pre-existing-stale (only lists langfuse + deploy, missing both `github` and `cost-monitor`) — outside this ticket's scope but left flagged for a future roster-touching ticket.", "updated_memory": ""}
